# This embedded snapshot is retired as a deployment source.
# Use AgenCi-MAIN/masterswitch/services/core-a2a-relay for releases.
# npm run dev selects wrangler.local.toml for historical local emulation.
name = "core-a2a-relay-platform-retired"
main = "src/index.ts"
compatibility_date = "2025-09-17"
workers_dev = false
preview_urls = false

[build]
command = "node scripts/retired-deployment.mjs"
