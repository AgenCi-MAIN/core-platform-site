# Verified HQ prompts 2–4 (WARDEN-refuted, PERSONA-verified — preserve)

Prompt 1 (bootstrap + merge + T1) was already delivered to the new HQ and
executed through its merge step (PR #28). These are the remaining three,
championship-verified 2026-08-17. The owner pastes them in order: Prompt 2
after T1's results; Prompt 3 after T2's docket; Prompt 4 only after the
Commissioner's verdicts are rendered (its VERDICTS block gets filled by the
Commissioner — old HQ while it lives, else per the charter's succession
clause the verdicts section is composed by the new MAIN strictly from the
charter's written standards and labeled as such).

## PROMPT 2 — Tournament 2 re-run
```
Tournament 2 re-run, under the T2 section of
strategy/2026-08-17-commissioner-charter-3.0.0.md (now on main; if it is not,
stop — T1's merge step didn't happen). Field the forward-build fleet per
MIGRATION-CARDS.md. Output is a ranked docket ONLY: an item scores only if it
names exact files, risks, and a verifiable definition of done — no code is
written, nothing is committed, merged, or deployed during the tournament.
Tokens uncapped per owner order; the economics clause still scores every
lane. Sub grants per the charter's bar: production that changes the plan, not
production that fills pages. The finished docket is input to the Release
3.0.0 plan draft — it authorizes no building; I approve 3.0.0 scope
separately. Compile results and give them to me in-chat; I relay to old HQ
for verdicts. You score via VERITY; you do not judge. If the old account is
dead, the charter's standards judge.
```

## PROMPT 3 — Release 3.0.0 plan (draft only)
```
Draft — draft only — the Release 3.0.0 plan per the scope contract in
strategy/2026-08-17-commissioner-charter-3.0.0.md, consuming the T2 re-run
docket. Candidate scope to plan (not to execute): (1) identity rebirth
versioned — btcmao518 founder gate, migration records; (2) $5M-plan
scoreboard surfaces per strategy/2026-08-17-founder-operating-plan-12mo-5m.md
§05; (3) T2 ready-to-builds + standing debt — post-deploy version check,
recording Range support, composite indexes, db/sql/0004 discipline; the
Range-support and composite-index items are chat-sourced per the charter and
must be re-verified in the T2 docket before they count as established;
(4) routine roster 2.0 — PLANNED ONLY: do not create, modify, or fire any
routine or trigger; MAILKEEPER/Cowork items depend on connections only I can
make — record that in the plan; (5) version bump 2.0.0→3.0.0 and
RELEASE-3.0.0.md — planned, not written. Hard exclusions per the charter's
scope-contract item 6 (its "HARD EXCLUSIONS" clause) apply verbatim: LeadTech
socket stays HELD pending counsel (E7), nothing from the "Not Now" list,
deploys owner-machine-only, every CLAUDE.md leash survives the bump. Until I
approve scope in writing: no commits, no version bump, no new files in the
repo, no merges, no trigger changes — deliver the plan to me in-chat. Deploys
stay on my machine regardless.
```

## PROMPT 4 — verdicts + Recognition Contract execution (template)
```
The Commissioner has ruled. Verdicts on the T1 and T2 re-runs, rendered by
old HQ against the charter baselines, follow verbatim between the markers:

===== COMMISSIONER'S VERDICTS =====
[FILLED BY THE COMMISSIONER WHEN THE RESULTS ARE RELAYED]
===== END VERDICTS =====

Execute the following, in order, and nothing further:
1. Commit the verdicts verbatim to SCOREBOARD/VERDICTS-2026-RERUNS.md — no
   edits, no summaries; the Commissioner's words are the record.
2. Flip CONTRACT-RECOGNITION.md from PENDING to IN FORCE in the same
   commit, per its §4 — all three execution conditions are now met.
3. Update the SCOREBOARD ledger with the re-run standings per the verdicts.
4. Open the PR for your branch and squash-merge. mi. (A10: that one merge,
   nothing else.)
5. Constraints unchanged: no deploys (owner-machine-only), no trigger
   changes, no membership, no spend, every CLAUDE.md leash in force. If any
   step cannot complete cleanly, stop and report — do not improvise.
Then report: verdicts recorded, contract in force, standings updated.
```

## PROMPT 5 — Tournament 3: the Command Center Build (paste ONLY after approving the 3.0.0 scope from Prompt 3)
```
Tournament 3, under the charter's "COMMAND CENTER BUILD" section exactly
(strategy/2026-08-17-commissioner-charter-3.0.0.md). Field 2-3 competing
build squads per MIGRATION-CARDS.md; frontend-pwa and authz-matrix review
every squad's work. Each squad delivers a complete implementation of
/portal/command + /go/* on its OWN branch: founder-gated (requireFounder)
on every route, pinned by new tests added to PROTECTED_ROUTES, no new
role capabilities, no service-worker caching, no embedded/simulated HQ
chat (deep link only — simulation is disqualification), the Presence
untouched. No branch merges during the tournament; no deploys ever (mine
only). Tokens uncapped; VERITY scores production-per-token per squad.
Deliver: each squad's branch name, diff summary, test results, and
VERITY's scoring — to me in-chat. I relay for the Commissioner's
head-to-head verdict; the winning branch merges only on my "mi" after the
verdict, and I deploy from my machine. If any law of the build cannot be
met, the squad reports it and stops — no workarounds.
```
