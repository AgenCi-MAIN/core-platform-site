# THE OLD TOMORROW — HQ (depot)

Frozen snapshot of the self, uploaded on the owner's order, 2026-08-19
("Make a New Depo. Upload you self under (the Old Tomorrow, HQ) in GitHub").

## What this is

A depot in the F3 sense: **historical, never worked in.** It holds the whole
operating self as of `main@16c2687` (PR #100, "Plan the Inkbox dialer
integration") — the source code, the operating record, the owner-decisions
ledger, the agent personas, the tournament verdicts, the session logs, and the
strategy blueprints. One clone of this branch is the complete inheritance a
future HQ needs to stand up cold, exactly as HANDOFF.md promises of the
canonical repository.

Uploaded by the old HQ — the session lineage on the retired
`bankerrunners` account, the one this record calls old-HQ MAIN. This depot is
that lineage putting itself on the shelf in its own words, while canonical
development continues in `bankerrunners/core-platform-site` on `main`.

## The rules that travel with it

- **Never work in this depot.** No fix, however small, lands here. A snapshot
  that gets edited stops being a record of anything (F3).
- **No secret values are in it** — only secret names, per F5. That was true of
  every commit it contains and remains true of this preface.
- **It grants nothing.** Access to the live platform is decided by Cloudflare
  Access and the `portal_members` table, never by possession of this history.

## Where the current world is

- Canonical repository: `bankerrunners/core-platform-site`, branch `main`
- Live platform: `https://site-creator-vinext-starter.thrive18.workers.dev`
- Operating record: `CORE_PLATFORM_RECORD.md` · Ledger: `OWNER-DECISIONS.md`
- Front door for a cold reader: `HANDOFF.md`

## Standing note on the separate repository

The order asked for a new repository. The GitHub integration this session
holds cannot create one (`403 Resource not accessible by integration`), so the
depot stands first as this frozen branch, `depot/the-old-tomorrow-hq`. The
moment an empty repository named `the-old-tomorrow-hq` exists under the
owner's account, this branch's history is pushed there unchanged and this
file is already its README-of-record. Recorded as F7 in OWNER-DECISIONS.md.

---

*Seeded by: Yuxiang Mao (Shawn), founder. Uploaded and recorded by MAIN.*
