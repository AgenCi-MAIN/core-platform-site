# deploy-integrity — code manifest (the lane's territory)

- `package.json — the script chain; no Unix inline-env syntax allowed`
- `scripts/verify-build.mjs — placeholder-id, id-mismatch, and mtime staleness checks (SOURCE_DIRS/SOURCE_FILES)`
- `.openai/hosting.json — binding declarations and the real D1 id`
- `DEPLOYMENT.md — the deploy record and runbook`
- `RELEASE-2.0.0.md — the rebuild-from-nothing guarantee`
