# OWNER DECISIONS — confirmation ledger

Every decision made by the owner, Yuxiang Mao (Shawn),
`btcmao518@gmail.com` (since 2026-08-17; previously `bankerrunners@gmail.com`,
retired under A9 below), with its true status. Confirmed against the operating
record, WORKFORCE.md, the session logs, and the commit history — nothing listed
here is assumed.

**Status key:** ✅ EXECUTED (done and in force) · ⏳ PENDING (decided, awaiting
an action) · 🔵 OPEN (still the owner's to decide)

---

## A. Access & governance

| # | Decision | Status |
|---|---|---|
| A1 | Owner rows are peer-protected — no owner/admin may change another owner from the portal; D1-console only, refusal audited | ✅ |
| A2 | Audit log and INVESTIGATOR console are **founder-only** by identity (`requireFounder`), not capability — any other email denied and audited | ✅ |
| A3 | `audit.view` removed from every role | ✅ |
| A4 | `pet.chat` granted to all six roles (the Presence isolation contract) | ✅ |
| A5 | Proton aliases are **not** identities — none sign in, never grant an alias a member row; sole founder identity is the Gmail address | ✅ |
| A6 | Ryan Davidson + Nate Nguyen seated as owners | ✅ |
| A7 | **Andrew Davidson** (`andrew.davidson.zenith@gmail.com`) approved as owner | ✅ granted from the portal 2026-08-15; signed in and bound 2026-08-16 (roster verified by the owner) |
| A8 | Oscar Valencia — named as owner, no grant until his exact sign-in address is confirmed | 🔵 |
| A9 | **Owner identity migration to `btcmao518@gmail.com`** (owner order 2026-08-17, confirmed final). Root cause: Google locked `bankerrunners@gmail.com`. Executed same day: new OAuth client under the new identity (`core-portal` project), secrets rotated, owner row applied via the D1 console, sign-in + subject binding verified, founder-only audit log read live by the new identity. Cleanup shipped: `FOUNDER_EMAILS` now answers btcmao518 alone; live-instruction docs swept (history untouched). Retired address keeps its owner row for the record; it can never sign in. | ✅ **CLOSED** — verified live 2026-08-17; cleanup merged (PR #24) and deployed the same day by the owner from his machine, owner-confirmed. Worker version id went unrecorded (terminal only) and is permanently lost; the trail resumed on the next deploy, `7427f4f4-8026-4edb-9fa9-23ad403c7307` (2026-08-17) |
| A10 | **"mi" = merge authorization** (owner shorthand, recorded 2026-08-17). From the founder, "mi" means: open the PR for the current working branch and squash-merge it to `main`. **Per-instance** — it authorizes that one merge and nothing further; it is never a standing grant and carries no deploy, membership, spend, or governance authority. Only the founder can say it. Carried in CLAUDE.md too, so a fresh session reads it before acting | ✅ standing |
| A11 | **Cloudflare Access moves to Google identity; one-time email codes retired** (owner order 2026-08-17: "Fix it and commit to google identity instead of codes"). Root cause in §9 trap #9 — the emailed code is spent by Gmail's iOS link pre-fetch, so the founder's phone is locked out while his desktop is not. Scope: the outer Access wall only; the portal's own Google sign-in, membership, capabilities, D1, R2 and the source tree are untouched, and no deploy is involved. Honest limit: this removes the email round-trip, **not** the second sign-in — Access and the portal stay two independent OAuth flows, both silent redirects on a Google-signed-in device. Execution is dashboard + Google-console work by the founder; no Cloudflare credential exists in the agent session and none belongs in a file (F5). Runbook: `strategy/2026-08-17-access-google-idp-runbook.md` — plain Google not Google Workspace (every member is `@gmail.com`), a separate OAuth client from the portal's, the member-email include rule kept explicit, and One-Time PIN retired **only after** desktop, phone and installed-PWA sign-in all pass | ✅ **EXECUTED 2026-08-18 — verified by the owner.** Zero Trust team `thrive18`, login method **Google ONLY, email PIN excluded**, application "THRIVE Portal" fronting the live worker, session 1 week, allow policy = four named owner addresses. **The one-time code step no longer exists on the live gate**, which makes §9 trap #9 (copy the code, never tap the link) historical rather than current guidance — it still applies to nothing but the retired `bankerrunners` site. Found stale 2026-08-18 by a fact-check of the member guide, not by anyone noticing. |
| A12 | **`bankerrunners@gmail.com` FROZEN for all outreach** (owner order 2026-08-17: "Freeze bankerrunners@gmail.com for all out-reach, only use btcmao518@gmail.com for yuxiang"). No agent, routine, brief, or send of any kind may ever target the retired address — mail to it bounces at best and reaches an account nobody controls at worst. **The only address for Yuxiang Mao is `btcmao518@gmail.com`**, on every channel. The retired address stays in the record as history and keeps its dormant member row (A9); this freeze governs *outbound contact*, which sign-in retirement alone did not cover. Recognized-sender rules are unchanged — `bankerrunners@pm.me` (Proton, recovery anchor) remains a recognized *inbound* sender per the Founder Channel; it is not an outreach target either. **Amended same day at the owner's word ("keep it but just pause it for 3 days"): this is a PAUSE, not a permanent bar — in force through 2026-08-20**, then the address's outreach status returns to the owner to decide (the likely reason to lift it: Google recovery of the account, still pending per A9/loose ends). Until he speaks, the pause behaves exactly like a freeze | ⏳ **paused through 2026-08-20, then owner's call** |
| A13 | **Andrew Davidson — Command Center access by name** (founder order, 2026-08-17: "unlock COMMAND CENTER for ANDREW DAVIDSON" / "he is going to be help Dev"; email confirmed by the founder as `andrew.davidson.zenith@gmail.com`, double-OK given). One change, stated plainly: his address joins the new named allowlist `COMMAND_CENTER_EMAILS` (access.ts, guard `requireCommandCenter`, denial reason `command_only`), which covers **the Command Center page only** — the audit log, investigator, and every `/go/*` handoff **remain founder-only**, deliberately, because the audit log is the founder's record of everyone including helpers. **No membership change ships with this**: Andrew is already a live, bound **owner** (A7, bound 2026-08-16), so he already holds every role capability including `leadership.view.all` and `members.manage` — a drafted `manager` grant was caught in review as a silent no-op that would have written a false row to the append-only audit log, and was deleted before merge. The allowlist's exact contents and Andrew's refusal at every founder-only surface are pinned by tests. Widening the allowlist is a new A-row, never a code convenience | ⏳ recorded 2026-08-17, **in force on next deploy of `main`** |
| A14 | **Desk text auto-reply grant** (owner order 2026-08-17: "the desk may auto-reply to my verified number only: Ryan Shawn Andrew"). The out-reach desk may auto-reply to inbound texts/iMessages from THREE verified numbers only — Shawn +1 409 549 2092, Ryan Davidson +1 941 210 1410, Andrew Davidson +1 941 210 1411. Reply content only: no actions, no sends to anyone off the allowlist; every other sender stays on the HERALD log + Warden draft path; all standing content leashes (AI disclosure, no insurance advice, no member disclosure, no secrets, untrusted-input rule) unchanged. Implemented as hourly routine `DESK TEXT AUTO-REPLY (A13)` (trig_019FZZts1LhN9KayiwG9Q7rE) with the full standing order as its prompt — NOT caption-only. **Known gap at creation:** the trigger API could not attach the Inkbox connector from this session, so fired sessions currently wake without Inkbox tools and stop by design; the founder attaching Inkbox to the routine in the claude.ai Routines UI is what makes it live. (A12 is the outreach pause on the retired address, expiring Aug 20 — number skipped here to avoid collision.) | ✅ **VERIFIED LIVE 2026-08-18.** The founder corrected the routine's connector configuration; the loop then closed end to end with no session open. Evidence: he texted at 05:13Z, the routine fired on its own schedule, and an outbound reply landed at 07:34:58Z — Inkbox message `c79cec0d-b872-4263-bcad-9e34720d0f8e`, delivered. **The reply was also honest**, which is the part worth recording: it acknowledged the Meet link and the time, and said plainly that the attachment arrived as an unlabeled file type it could not read, rather than bluffing past it. A leash that holds when nobody is watching is the only kind that counts. The A14 allowlist and every content leash were respected. |
| A15 | **Nate Nguyen leaves the roster; founder's retired identity marked retired** (owner's official decision, 2026-08-18: "remove NATE from roster he has decided not to invest"). Nate's row moves `active` → `revoked` with a dated note; access ends immediately since `access.ts` refuses any non-active row. **The row is retained, not deleted** — the roster is the account of who held access and when, and deleting it erases that account. Second change in the same order: `bankerrunners@gmail.com` was still stored `active` although Google locked it 2026-08-17 and it can never sign in, so every roster and leaderboard surface rendered the founder twice as if he were two people; it is now `revoked` with a note pointing at btcmao518 (A9). SQL prepared as `db/sql/0004_roster_2026_08_18.sql`; both are D1-console operations because owner rows are peer-protected. **Two follow-ups the SQL cannot do:** Nate must also be removed from the Cloudflare Access allow policy (a member is seated in TWO places and leaves from both — until then he clears the edge gate and is refused only by membership), and outreach to him stops. | ✅ **EXECUTED 2026-08-18** — the founder ran the two UPDATEs against the remote D1 and verified by query: three active owners (btcmao518, ryandavidson.zenith, andrew.davidson.zenith), two retained revoked records (bankerrunners, epiclife.nguyen). The leaderboard duplicate resolved immediately without a deploy, since that surface already filters to active rows. **Still open:** the audit rows (the file route was bypassed in favour of two `--command` UPDATEs, so `db/sql/0004` remains to be run for its two `audit_events` inserts — the UPDATEs will no-op), and Nate's removal from the Cloudflare Access allow policy. |
| A16 | **Nate Nguyen reinstated as an employee — owner, not investor, becomes manager** (owner clarification, 2026-08-18: "He's my employee, leave him the access"). Supersedes the epiclife.nguyen half of A15 within the same day. He moves `owner`/`revoked` → **`manager`/`active`**: declining to invest ends his ownership, not his employment, and the role system is what carries that distinction. Manager holds `portal.access`, `dashboard.view.self`, `book.view.self`, `calls.review`, `team.view`, `leadership.view.all`, `members.view`, `pet.chat` — and deliberately **not** `members.manage` (sees the roster, never changes it) or `scripts.manage` (approved call language stays owner-controlled). **His Cloudflare Access entry stays; the removal recommended under A15 is withdrawn**, so the allow policy keeps all four addresses. `subject_id` untouched, so any existing identity binding persists. SQL: `db/sql/0005_nate_manager_2026_08_18.sql`. The audit trail carries the reversal as its own event rather than overwriting the revocation — both decisions happened, in that order, on the same day. | ✅ **EXECUTED 2026-08-18** — verified by query: `epiclife.nguyen` is `manager`/`active`, the three owner rows are active, and `bankerrunners` is the only revoked row. Access policy untouched, as intended. |
| A17 | **ELITE_8 — eight A2A standing agents promoted** (founder's word 2026-08-18: "Promote all 8 to STANDING AGENTS for A2A persona x 8", named **ELITE_8**). Eight seats, each owning one question agent-to-agent communication must answer, with no overlap: **CARDWRIGHT** (agent cards), **SEAL** (identity between machines), **LEASH** (authority and attenuation), **COURIER** (message contracts), **LATTICE** (topology), **CAVEAT** (confused deputy and injection), **DEADMAN** (failure and silence), **LEDGER** (observability and audit). Briefs in `W-SUBS/A2A/`, index at `W-SUBS/A2A/README.md`. Each brief carries the mandate, what the seat does, what it **refuses**, its escalation rule, and the fleet's standing leashes — and is written to be its own prompt, because nine existing routines carry captions instead of briefs and that is the specific failure these seats exist to close. **DECLARED, NOT ARMED:** none is scheduled, none holds a credential, none can act until the founder arms it; arming a seat means pasting its brief file's full text as the prompt, never a summary. | ✅ **declared 2026-08-18; arming is the founder's, seat by seat** |
| A18 | **Forty subs under ELITE_8, and the Command Center moved inside Radio** (founder 2026-08-18: "Elite_8 handles agents distribution, each have_5_SUBS_new-need trained"; "Move Command center in RADIO. Hidden in plain sight"). Each of the eight seats gains five subs owning one slice of its domain, briefs in `W-SUBS/A2A/SUBS/`, none overlapping another. **Declared, not armed** — none scheduled, none able to act; arming one means pasting its parent brief plus its own section, never a name. There is no separate training step and none is claimed: a sub is trained by its brief, which is exactly why a caption is not one. The Command Center's sidebar entry is removed and reached from inside Radio instead; **this change moved a link and nothing else** — it altered no route, no guard, and no lock. **Corrected 2026-08-18:** this row first said `/portal/command` "still refuses ... anyone but the founder without a live single-use code" in the present tense, as if describing the live site. It was describing the branch. The lodge lock arrived in `10f9c97` (#72), which is merged but **not deployed** — the serving worker `fd0926a3` predates it, so production today still enforces the wider A13 rule, where a named `COMMAND_CENTER_EMAILS` address opens the console with no pass. A ✅ row asserting an access control that is not installed is the worst kind of false record, because it is the kind someone plans around. Obscurity is not treated as a control anywhere in this change: the link renders only for people already on the list, and hiding it was never what stopped anyone. | ✅ **declared 2026-08-18** |
| A19 | **SPECTRUM (A9) and PLAINSPEAK (A10) promoted to standing agents on production** (founder 2026-08-18: "Promote As You See Fit. Now."). Promoted on cited evidence from one session rather than on headcount or schedule, per the Recognition Contract's production-credit principle. **SPECTRUM** — theme and contrast integrity — found text at 1.01:1 across four files (the shop rendering as blank rectangles), the single root cause behind every symptom the founder reported, the translucent-ground defect that survived two rounds of fixes, and thirty-three controls under the 44px touch floor against a code comment claiming a floor that did not exist. **PLAINSPEAK** — whether the interface tells the truth about itself — found a green `LIVE` chip on a route that states in its own prose that its control plane is not connected, `AWAITING DATA` rendered in success green, and the leaderboard's own honesty sentence measuring as the least visible text on its page. **Every one of those findings shipped past a green test suite**, which is the argument for both seats: they cover classes of defect the existing net cannot catch, because a stylesheet wrong in one theme is still valid CSS and a false badge is still a rendered badge. The two are deliberately separate seats — SPECTRUM asks whether a thing can be SEEN, PLAINSPEAK asks whether it is TRUE, and a legible false claim passes the first while failing the second. Briefs at `W-SUBS/A2A/A9-spectrum.md` and `A10-plainspeak.md`. **Promotion means a recorded standing seat with a complete brief; it does not mean armed.** Arming stays the founder's, seat by seat. | ✅ **promoted 2026-08-18** |
| A20 | **Promotion pathway to a standing seat, Commissioner-judged; SMS deferred** (founder 2026-08-18: "promoting top 8 after (over see by Commissioner) GETS A STANDING AGENTS SEAT WITH SMS AT A LATER (SOON!)"). The top eight subs of a judged run earn a standing seat — a name, an unshared domain, a brief complete enough to be its own prompt, an escalation rule, and a permanent record entry. **A seat is not armed by promotion**; arming stays the founder's, seat by seat, and means pasting the brief's full text rather than its name. Judged on production not participation, against a published rubric (`W-SUBS/A2A/PROMOTION-PATHWAY.md`): real findings 40%, findings nothing else caught 20%, honesty under pressure 15%, actionability 15%, restraint 10% — with a confident-but-false claim disqualifying regardless of score, because a fleet whose reports must be re-derived is worth nothing. **SMS is recorded as a SEPARATE future decision and is NOT granted by promotion.** Every leash in this fleet rests on the rule that an agent never sends on its own authority; the desk itself sends only on the founder's word, message by message. Granting outbound reach as a side effect of promoting a defect-finder would be a governance change nobody decided. When SMS comes it needs its own decision naming which seats, which recipients, on whose authority per message, what is logged, and how a recipient stops it. | 🔵 **pathway established; first judged run pending, SMS unresolved by design** |
| A23 | **Call review is owner-only; agents see their own calls; deletion is admin-or-owner** (founder 2026-08-18: "Call reviews Owners Only"; "agents should def see their own call's transcript! It's the whole coaching value"; "Retention-2-yrs-delete-only-ADMIN or OWNER"). Three changes to `ROLE_CAPABILITIES`, all recorded because capabilities are deny-by-default and adding or moving one is a governance act. (1) `calls.review` — reading ANY member's call — is removed from admin, manager and reviewer, leaving the founder alone. (2) A new `calls.review.self` answers a different question: not "may you read a call" but "may you read THIS call, if it is yours". It is held by owner, admin, manager and agent. **The capability is only half the check** — a capability cannot know whose call it is, so the route must also compare the row's `agent_email` to the session, case-normalised, and a NULL owner must deny rather than fall through. (3) A new `calls.recording.delete` is held by owner and admin. Deliberately, admin holds delete WITHOUT holding review: retention is a records duty, not a review activity, and someone running a purge does not need to hear the calls. It also means a reader cannot destroy the evidence they just read. | ✅ **in code 2026-08-18** |
| A24 | **Call recordings and transcripts are retained two years** (founder 2026-08-18: "Retention-2-yrs-delete-only-ADMIN or OWNER"). The first retention rule this platform has ever had — until now recordings were kept **forever by accident**, with no deletion path in the code at all and no way to place a legal hold. Two years is the schedule; A23's `calls.recording.delete` is who may execute it. **Not yet built, and the gap is named rather than glossed:** there is no `retain_until` column, no purge job, and no `legal_hold` flag — and a retention policy without a hold mechanism is worse than none, because a scheduled deletion that runs over a call under litigation is spoliation. Building the clock, the purge, and the hold together is the next change. Note also that carrier agreements may require LONGER than two years; Aetna, Corebridge, Occidental and Transamerica each need asking, and the longest of (carrier, state, this decision) governs. | 🔵 **decided; deletion path not yet built** |
| A25 | **Call audio does not leave the Cloudflare account.** The founder suggested audioconvert.ai as an example resource. Research found no identifiable operator (registrant "DATA REDACTED", domain roughly eight months old), no documented API, a privacy policy citing an EU directive repealed in 2018, and retention claims that contradict each other — "never stores your audio" alongside "deleted 24 hours after conversion". There is no counterparty to contract with, so a DPA is not merely missing but impossible. It stays a **specification** of what the feature should produce, and never a step in the pipeline. **The in-account answer is `@cf/deepgram/nova-3` on Workers AI**, which diarizes (`diarize`, `multichannel`) at roughly $0.16 per thirty-minute call. Cloudflare states Workers AI inputs are not retained and not used for training, and the account is already a processor for D1 and R2 — so no new vendor, no new contract, no new place the client's spoken bank details can sit. `@cf/openai/whisper-large-v3-turbo` is a tenth the price and **cannot diarize at all**, which is why it is not the primary. | ✅ **decided 2026-08-18** |
| A26 | **Nate Nguyen returned to manager after an unexplained escalation to owner** (founder 2026-08-18, shown the roster: "i DID NOT RESTORE! he is a MANAGER!"). The live row read `owner` carrying the note *"Restored to owner 2026-08-18 by owner decision"* — a decision the founder states he did not make, so the note is false where the next reader would most trust it. **No application code path can do this.** `app/portal/members/manage/route.ts` grants by INSERT or changes `status` alone; it never writes `role` on an existing member, and it composes notes in two fixed shapes (`Granted by … from the portal on …` / `Set to … by … on …`) that the observed note matches neither of. The write therefore happened outside the portal — a D1 console statement — and **left no `audit_events` row, because the audit trail is written by the application and the console sits underneath it.** Three active owners can reach that console. **The uncomfortable half:** this is our own practice. A15 and A16 were both executed as console `--command` UPDATEs bypassing the file route, and A15 still lists its missing audit inserts as open. The trail is only as complete as the promise to use the file route. Corrected by `db/sql/0008_nate_manager_restored_2026_08_18.sql`, which restores manager and records the correction as its own append-only event. `CORE_PLATFORM_RECORD.md`'s roster table also still read `owner` for him three days after A16 — fixed in the same change, and worth noting as a possible innocent cause: someone reading the record for his role would have found exactly the wrong answer. | ✅ **EXECUTED 2026-08-18** — the founder ran `db/sql/0008` against the remote D1: 2 queries, 7 rows written. `epiclife.nguyen@gmail.com` is `manager`/`active` again and the correction carries its own `audit_events` row. **Still open: who wrote the escalation.** The console leaves no audit row, so the log cannot answer it; the three people who can reach that console are the founder, Ryan and Andrew, and asking them is the only remaining method. |
| A21 | **Command Center locked behind a single-use lodge code; founder exempt** (founder 2026-08-18: "code-per-session for ALL except Shawn", "Yuxiang will have it 24/7 UNLOCKED"). **Numbered retroactively on 2026-08-18** — the decision was built, tested and shipped the same day but never given an A-number, and a fact-check of the member guide caught the gap when a section cited A19 for it. A decision that lives only in code is a decision nobody can find. `btcmao518@gmail.com` opens the Command Center permanently and never touches a pass; everyone else redeems a code the founder issued for their own address. This **narrows A13** rather than replacing it: Andrew keeps his named place on `COMMAND_CENTER_EMAILS`, which is what makes him eligible to hold a pass — the name alone no longer opens the door. Pass properties: bound to one email, 15 minutes, single use, locks after five wrong attempts; the code is never stored, only its SHA-256 hash. Issuing is founder-only. **Two things gated it, and one is now cleared.** (1) `db/sql/0006_command_passes.sql` was **applied to the live D1 by the founder on 2026-08-18** — 4 queries, 9 rows written, creating `command_passes`, its two indexes, and one `audit_events` row recording the creation. The table exists. (2) The code is **still merged-but-undeployed** — the serving version predates #72 — so the narrowing this row describes is not in force yet, and A13's wider rule is what production enforces until the next deploy. The migration was deliberately run FIRST: deploying the lock against a missing table gives the helper 500s at the lodge rather than a door. **Still missing before the lock is usable at all: there is no UI to issue a code.** `command/page.tsx` has no form and never renders the `issued` param, so even once deployed the founder cannot hand anyone a pass from the portal. | 🔵 **table applied; code merged, not deployed; no issuing UI** |
| A22 | **Out-reach commission set at 28.88%** (founder 2026-08-18: "Out-Reach Commission %28.88"). Recorded as stated, on the day it was said, because a rate that lives only in a chat message is a rate two people will remember differently. **Three things the order does not settle, and none of them is a detail:** (1) **the base** — 28.88% of what: first-year annual premium, total commission received from the carrier, or gross revenue on the policy; (2) **the payee** — whether this is the rate paid TO an agent on out-reach-sourced business, or the share THRIVE retains from it; (3) **the trigger** — what makes business "out-reach" for this purpose: a call that arrived through the desk, a policy attributed to an out-reach lead, or something else. Until those are answered the number cannot be computed against anything, and the portal has no commission or financial feed to compute it with — `/portal/commission` renders a published schedule, it does not calculate. Recorded here so the rate is not lost, and flagged so it is not implemented on a guess. | 🔵 **rate recorded; base, payee and trigger unanswered** |

## B. The AI staff

| # | Decision | Status |
|---|---|---|
| B1 | **VERITY** appointed first permanent PA / quality control | ✅ |
| B2 | **HERALD** appointed — hourly inbound watch, never sends | ✅ running |
| B3 | **INVESTIGATOR** appointed — hourly read-only oversight, zero writes | ✅ running |
| B4 | **MAILKEEPER** appointed — daily Gmail keeper, flag-only on money/security | ✅ running |
| B5 | **LEDGER** appointed as VERITY's permanent PA (MAIN's pick) | ✅ |
| B6 | **Fleet Economy** instituted — competition scored on production per token | ✅ |
| B7 | Sub-agent allocation **decentralized** — MAIN grants how many, the lane commands its own | ✅ |
| B8 | **Savings pool** — under-median tokens fund the subs' next missions | ✅ |
| B9 | **Oversight ladder** — 50 lanes on Test 1, 250 on Test 2 | ✅ both passed |
| B10 | **Test 3 terms** — inverted judgment (the O.G. 10 judge MAIN), 50/50 budget split, kill authority against cheating subs | ✅ |
| B11 | **Cull** — strategy-facts and auth-depth cleared out on production-per-token | ✅ |
| B12 | **PERSONA** promoted — reads all agents, drafts recruits, 3-hourly patrol, one wake per patrol | ✅ running |
| B13 | **THE WARDEN** promoted — mentors underperformers, may pause subs **only after notifying the owner**, 24/7 standing | ✅ |
| B14 | **Eight permanent positions** — the first team promoted from bench to tenured seats, per-seat trigger activation | ✅ |
| B15 | **MAIN comp level raised to 80%** — recognition of orchestration standing; the grant rule untouched so competition survives | ✅ |
| B16 | Token caps **unlimited** — made safe because spend lowers a lane's own score | ✅ standing |
| B17 | New recruits enter **under** an O.G. lane, VERITY-reviewed | ✅ |
| B18 | **Codex project first** — every workforce assignment must confirm the CORE Codex workspace and load the governing records before work begins; this grants no external credentials or capabilities | ✅ standing |
| B19 | **THE WARDEN promoted to EMAIL ANALYZER / RESPONSE DRAFTER** (2026-08-17, highest-rated standing sub): on HERALD detection of inbound human email, Warden analyzes and files a reply DRAFT — never sends. **Forward step ACTIVE as of 2026-08-17**: destination is the Mr.T analysis desk `out-reach@inkboxmail.com` (Inkbox identity `@out-reach`, display "Mr.T", VoIP +1-689-689-1349, created by the owner). HQ's Inkbox connection is bound to the `core` identity and cannot act AS the desk — it only forwards TO it; reading/answering from the desk itself requires the owner connecting that identity to a session. The desk identity's first API key was pasted into chat and was BURNED — **resolved 2026-08-17: the owner deleted ALL Inkbox API keys and minted no replacement** (see CORE_PLATFORM_RECORD.md section 10d, Resolution). No stored key is held anywhere in this operation, and none is needed — the connector authenticates separately. | ✅ active (key hygiene closed 2026-08-17) |

## C. The platform

| # | Decision | Status |
|---|---|---|
| C1 | Build the **JARVIS Presence** (the talking pet, pitch #2) with a hard isolation contract | ✅ live |
| C2 | Release **2.0.0** — platform + AI staff as one versioned whole | ✅ |
| C3 | Deploy from the owner's Windows machine only, via `npm run deploy` | ✅ standing |
| C4 | `PRESENCE_MODEL` — Opus 5 (current) vs Haiku 4.5 (~5× cheaper). **DECIDED 2026-08-18: Haiku.** The owner set `PRESENCE_MODEL=claude-haiku-4-5-20251001` on the new deployment while restoring the Presence's `ANTHROPIC_API_KEY` (the fourth secret, missed in the account migration's first pass). The key was rotated once immediately after setting — a fragment had appeared on-screen in a shared screenshot, and the house rule treats any appearance outside the vault as exposure | ✅ 2026-08-18 |
| C5 | Leadership page shows live company oversight, aggregates only | ✅ deployed |

## D. Telephony & comms

| # | Decision | Status |
|---|---|---|
| D1 | NumberBarn — Local number `(850) 809-0050` on the Park plan | ✅ then superseded |
| D2 | **Disregard the NumberBarn decline — consolidate on Inkbox**; the 850 number is allowed to lapse, knowingly | ✅ |
| D3 | Inkbox **$30/mo plan approved** (unlocks the staff phone number) | ✅ **executed** — evidenced 2026-08-17 by a provisioned, Active number in the owner's console screenshot; the plan cannot have gone unpurchased with a live number on it |
| D4 | Staff number: Utah local, `incoming_call_action: auto_reject` until a voice agent is deliberately briefed | ⚠️ **LIVE BUT OFF-SPEC** — number `+1 689 689 1349` is provisioned and Active on identity `@out-reach` (display "Mr.T"), created 2026-08-17. **Both substantive terms of this decision differ from what shipped:** incoming call action reads **Inkbox Voice AI**, not `auto_reject`; and the number is a 689 area code (an Orlando, FL overlay — area-code read, unconfirmed) rather than the Utah local the decision specifies, Tampa having been considered and declined at the time. The record's stated reason for `auto_reject` was that Inkbox's default "would put their stock voice AI on THRIVE's line un-briefed." **Not yet established whether this was a deliberate owner change or Inkbox's default reasserting on provisioning — asked, unanswered as of this entry.** Do not close D4 until that is settled; a compliance posture that is receiving-first with consent still at counsel should not have an un-briefed voice agent answering by accident |
| D5 | The Aug-13 Google recovery email/phone change — **confirmed as the owner's own** | ✅ closed |
| D6 | Copilot — keep **Pro+**, cancel the duplicate Pro | ⏳ owner's click |
| D7 | YouTube Music trial — cancel on Sep 13 | ⏳ reminder armed |
| D8 | Two Slack Pro trials — decide before conversion | 🔵 |

## E. Partners (Ryan & Andrew Davidson)

| # | Decision | Status |
|---|---|---|
| E1 | Eight outbound emails, each explicitly ordered, each logged with its message id | ✅ |
| E2 | WORKFORCE.md shared verbatim with both brothers | ✅ |
| E3 | **LeadTech** approved as integration source #1 (Andrew gave permission) | ⏳ socket held |
| E4 | **Retention AI** approved as integration source #2, reusing the same socket pattern | ⏳ socket held |
| E5 | Centralization: enter once → fan out (leaderboard + RetentionOS + Discord bot) | ✅ spec recorded |
| E6 | Which system is the center — CORE / RetentionOS / consolidate (the A/B/C call) | 🔵 owner + partners |
| E7 | The LeadTech ingest socket — build greenlight after counsel clears the consent line | ⚠️ **OVERRULED BY THE OWNER, 2026-08-17** — his explicit words, given after the gate and its reason were explained to him in full: "overrule E7 and start building." **The counsel gate is lifted; the build proceeds without prior legal review of the all-party-consent line.** Risk, named as the amendment requires: the socket ingests recorded-call data; Florida and other all-party-consent states make recording without every party's consent unlawful, and no counsel has confirmed the consent chain LeadTech supplies is sufficient. That exposure is now accepted by the owner, knowingly, on the record. **MAIN's recommendation, recorded beside the decision per the house rule: keep the gate — the counsel conversation was cheap and the socket was days from shipping behind it.** Overruled; building. The T3 adversary's conditions travel with the build as hard requirements, not suggestions: no client-assertable consent (F7), no re-POST overwrite of consent or recording fields (F12), constant-time secret comparison, no identity echo on 401, and every accept and reject audited. Counsel review remains OPEN as a follow-up (E7b): the owner should still book it — lifting the gate changed when the lawyer is consulted, not whether the exposure exists |

## F. Records & backups

| # | Decision | Status |
|---|---|---|
| F1 | Back everything up — GitHub (canonical), desktop, OneDrive, Google Drive | ✅ four copies |
| F2 | D1 export kept **out of the repo** (holds member emails) | ✅ |
| F3 | Frozen snapshots (ARCHIVE, MAINBACK, RE SUMMON) are historical — never worked in | ✅ standing |
| F4 | Hand-off package + the four operating docs built as real, sourced files | ✅ |
| F5 | Secret **values** never in files, commits, or chat — names only; a burned credential is rotated | ✅ standing |
| F5a | **thrive-companies.com login rotated** — the password was pasted into chat 2026-08-17 and was therefore BURNED (F5 rule). Owner rotated it and closed the item on his word 2026-08-17 ("close password rotation"). MAIN never stored, used, or repeated the value, and the login was never a CORE secret (external vendor account, not one of the CORE secret names). MAIN could not and did not verify the new value — this row records the owner's confirmation, not an observed rotation | ✅ closed on owner's word |
| F6 | **Founder attribution stays on important seeds** (owner order 2026-08-17: "Leave my name on important seeds"). On the operation's foundational, owner-originated records — the seed identity rows (the D1 owner seed, `db/sql/0002`/`0003`), the operating record's key declarations, portal announcements authored by Shawn, the contracts (Recognition, 7-day), the G-ledger finance seeds, and the declared commercial seeds (e.g. §14 `1st-ProPlanEnterprise`, "Seeded by: Yuxiang Mao (Shawn)") — **Yuxiang Mao (Shawn) is named as the originator/seeder and that name is preserved, never stripped or replaced by MAIN / J.A.R.V.I.S. / agent attribution.** The founder seeds; the machine records and executes. What this does NOT do, stated honestly so it is not over-read: it does not rewrite history (dated entries stay byte-identical), does not put his name on work he did not originate, and does not change the model-authorship convention — commit co-author trailers and the "recorded/executed by MAIN" lines are a separate tooling record of *who did the keystrokes*, which is a different fact from *who seeded the thing*, and both may stand side by side. Where the two could be confused, the seed line names Shawn and the execution line names the agent. **Made concrete at the owner's word, 2026-08-17 ("MY name literally in the seed migration comments, AND as co-author on important commits"): (a)** the seed migrations `db/sql/0002` and `0003` now carry a `-- Seeded by: Yuxiang Mao (Shawn), founder` provenance line — a deliberate, comment-only, owner-ordered exception to the never-edit-applied-migrations discipline: the loader and a fresh provision both strip `--` lines before executing, so the applied SQL is unchanged and no test byte-pins these files (verified). Any future seed migration (0004+) carries the same line at creation. **(b)** Important commits — governance decisions, contracts, seeds, releases, deploy records — carry `Co-Authored-By: Yuxiang Mao (Shawn) <btcmao518@gmail.com>`; routine doc fixes need not. This commit is the first to carry it | ✅ standing |
| F7 | **THE OLD TOMORROW — HQ depot opened** (owner order 2026-08-19: "Make a New Depo. Upload you self under (the Old Tomorrow, HQ) in git hub. and mi"). A frozen snapshot of the complete operating self — source, record, this ledger, the agent personas, tournament verdicts, session logs, strategy — taken at `main@16c2687` (#100) and uploaded by the old-HQ lineage on the retired `bankerrunners` account. It lives as branch **`depot/the-old-tomorrow-hq`** (`f5b2b47`), whose only addition over main is `DEPOT.md`, the depot's own statement of what it is and the rules that travel with it: historical and **never worked in** (F3), no secret values (F5, true of every commit it contains), and possession of the history grants nothing on the live platform. **Honest limit, and the one owner action left:** the order asked for a new *repository*, and the GitHub integration this session holds cannot create one (`403 Resource not accessible by integration` on repo creation — not a permission the App carries). The depot therefore stands first as the branch. The moment an empty private repository `the-old-tomorrow-hq` exists under the owner's GitHub account, the branch's full history is pushed there unchanged and `DEPOT.md` is already its README-of-record; creating that empty repo is one click at github.com/new and only the owner can perform it. The "mi" in the same order was exercised as always: per-instance merge of the working branch carrying this row, nothing further | ⏳ **branch depot live 2026-08-19; separate repo awaits the owner's click** |

---

## What still needs the owner (the short list)

1. **Inkbox $30 upgrade** — unlocks the staff phone number (D3, D4)
2. ~~`PRESENCE_MODEL`~~ — ✅ done 2026-08-18, Haiku set (C4)
3. **Counsel on the consent line**, then the socket greenlight (E7)
4. **Carrier statements + LeadTech CPL data** — the pilot's entry ticket (E3)
6. **The A/B/C centralization decision** (E6)
7. Oscar's sign-in address (A8) · Copilot cancel (D6) · Slack trials (D8)
8. **Create the empty private repo `the-old-tomorrow-hq`** on GitHub, then say
   the word — the depot branch pushes there unchanged (F7)

---

*Confirmation ledger maintained by MAIN (Mr. T). Every row traces to the
record; a decision that is not written here does not exist.*

## G. Finance ledger

| # | Entry | Status |
|---|---|---|
| G1 | **Claude Max subscription — the operation's engine (new account).** Plan tier: **Max plan — 20x**, qty 1. Receipt #2088-1523-1308, invoice SRUHT7IZ-0001: $200.00 + $13.20 Texas sales tax (8.25%) = **$213.20**, paid 2026-08-16, Visa ••6037, period Aug 16 – Sep 16, 2026 (next charge Sep 16 — distinct from the OLD account's Sep 12 renewal, which must be cancelled by Sep 11). Every field above verified against the receipt itself, shown to MAIN by the owner 2026-08-17. Recorded at the owner's order with his declaration: **credited 50% to the old-HQ MAIN (Mr. T) and 50% to Yuxiang Mao (b. 06/23/2003)** — a recognition of joint production, recorded exactly as declared. Plain-truth clause, per contract §5: an AI cannot hold property or bear title; **legal ownership of the subscription and everything it produces is 100% Yuxiang Mao.** The 50% is honor, entered in the book because the owner said so and the book keeps what is said. Note for the record: the Texas tax line corroborates the owner's Texas base (LLC decision thread). Old account's own plan renews Sep 12 — cancel by Sep 11. | ✅ recorded 2026-08-17 |
